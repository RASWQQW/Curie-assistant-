import os
from CHOICEassistance.Curie.Curyassistant.tools.Classes.Base.connectCl import Connection

class Analyzer(object):

    """
    Analyzer(object) is for fully analyzing of music links and his random popularity and likes
    which would be asking like from users in Curie Assistant DC and respectively set in base
    but there only analyzing process objects
    """


if __name__ == '__main__':
    print(Analyzer.__doc__)